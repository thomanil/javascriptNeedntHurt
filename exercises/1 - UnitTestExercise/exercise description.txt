basic:

In test.js, the testAdd() function tests the UnitTestExercise.addNumbers() method.
Run the testcase (open 'test.html' in Firefox). Verify that the test fails.
Make the test succeed by implementing UnitTestExercise.addNumbers()

bonus:

Write tests and implementations for subtractNumbers(), 
divideNumbers() and multiplyNumbers() methods.
